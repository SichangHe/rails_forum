<article>
  <h2 class="text-4xl">Resend unlock instructions</h2>
  <%= form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post }) do |f| %>
    <%= render "devise/shared/error_messages", resource: resource %>
    <div class="my-5">
      <%= f.label :email %>
      <%= f.email_field :email, autofocus: true, autocomplete: "email", class: 'form text_field' %>
    </div>
    <div class="inline">
      <%= f.submit "Resend unlock instructions", class: 'inline-block btn-to-submit' %>
    </div>
    <%= render "devise/shared/links" %>
  <% end %>
</article>
