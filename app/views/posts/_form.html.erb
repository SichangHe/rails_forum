<%= stylesheet_link_tag "actiontext", "data-turbo-track": "reload" %>
<%= form_with(model: post, class: 'contents') do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation" class="px-3 py-2 mt-3 font-medium text-red-500 rounded-lg bg-red-50">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>
      <ul>
        <% post.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="my-5">
    <%= form.label :title %>
    <%= form.text_field :title, value: @post.title == 'Untitled' && '' || @post.title, class: 'form text_field' %>
  </div>
  <div class="my-5">
    <%= form.label :content %>
    <%= form.rich_text_area :content, rows: 4, class: 'form text_field' %>
  </div>
  <div class="my-5">
    <%= form.label :status %>
    <%= form.select :status, options_for_select(%i[PUBLIC PRIVATE]), {prompt: "choose visibility"}, class: 'form text_field' %>
  </div>
  <div class="my-5">
    <%= form.label :tag_list %>
    (separated by spaces)
    <%= form.text_field :tag_list, class: 'form text_field' %>
  </div>
  <div class="inline">
    <%= form.submit class: 'inline-block btn-to-submit' %>
  </div>
<% end %>
